export const itemNotFound = (columns: string) => `
Copy Paste Master can not find item column: "${columns}".
Check recipe if column name are input correctly.`;

export const subItemNotFound = (columns: string) => `
Copy Paste Master can not find subitem column: "${columns}".
Check recipe if column name are input correctly.`;
